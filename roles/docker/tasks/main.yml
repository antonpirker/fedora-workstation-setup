- name: Install Docker
  dnf:
    name: docker
    state: present

- name: Enable Docker
  systemd:
    name: docker
    enabled: true
    state: started

- name: Add user to docker group
  user:
    name: "{{ user_name }}"
    groups: docker
    append: true